{
    "scripts": {
        "server": "npm run dev-server --prefix server",
        "client": "npm run dev --prefix client",
        "root-deps": "npm install",
        "server-deps": "cd ./server && npm install && cd ..",
        "client-deps": "cd ./client && npm install && cd ..",
        "setup": "npm run root-deps && npm run server-deps && npm run client-deps",
        "db-populate": "cd ./server && npm run populate && cd .."
    }
}
